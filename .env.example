# SMTP configuration for intake form emails
# Copy to .env and fill in your credentials (never commit .env)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@yourdomain.com
SMTP_PASS=your-app-password

PORT=3000
